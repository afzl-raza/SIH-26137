def load_osm_network(place_name: str = "Sioux Falls, South Dakota, USA"):
    """
    Documented stub for live OpenStreetMap network ingestion.
    Requires `osmnx` package and live internet access.
    """
    raise NotImplementedError(
        f"OSM network ingestion for '{place_name}' requires the 'osmnx' package "
        f"and active internet connectivity. Use build_sioux_falls_real() for offline benchmark operations."
    )

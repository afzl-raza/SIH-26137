# Leave unset for local dev (Vite proxies /api to http://localhost:8000, see vite.config.js).
# When frontend and backend are deployed separately (e.g. Vercel + Render),
# set this to the deployed backend's URL, no trailing slash.
# VITE_API_BASE_URL=https://sih26137-backend.onrender.com
